rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() { return request.auth != null; }

    // Internal-only storage. Customer never reads Storage directly.
    match /{allPaths=**} {
      allow read, write: if signedIn();
    }
  }
}
